{% include '_partials/_head.twig' %}

<body class="bg-zinc-50">
{% include '_partials/_nav2.twig' %}
<div class="my-20 max-w-6xl mx-auto px-4 prose md:prose-lg min-h-screen">
    <div class="grid md:grid-cols-2 md:gap-10">
	<div>
		<h1>{{ entry.title | widont }}</h1>


		{{ entry.formattedText }}

	</div>
    <div class="not-prose">
        {{ craft.formie.renderForm('donate') }}
    </div>
    </div>
</div>

{% include '_partials/_footer.twig' %}
